{/* 

   import React from "react";

const SearchBar = ({ searchText, setSearchText }) => {
  return (
    <div className="flex justify-center my-6 grid-cols-1 sm:grid-cols-2">
      <input
       type="text"
        placeholder="Search Block / Line / Operator"
        className="border px-4 py-2 w-80 text-lg rounded shadow"
        value={searchText}
        onChange={(e) => setSearchText(e.target.value)}
     />
    </div>
  );
};


=============================================  THIS COMPONENT IS NOT IN USE   =================================

export default SearchBar;   */}